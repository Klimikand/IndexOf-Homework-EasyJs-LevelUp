
// 6. //indexOf - Implement the indexOf function for arrays.

    function indexOf(array, elem) {
        let result = array.indexOf(elem);
        return result;
    }

    let result = indexOf([1, 2, 3], 1)
    console.log(result)
    


// Проверка
// indexOf([1, 2, 3], 1)               // 0
// indexOf([1, 2, 3], 4)               // -1